import { OnviewportDirective } from './onviewport.directive';

describe('OnviewportDirective', () => {
  it('should create an instance', () => {
    const directive = new OnviewportDirective();
    expect(directive).toBeTruthy();
  });
});
